{% extends 'base.html.twig' %}
{% block title %}Comment-user{% endblock %}
{% block body %}

<div class="container  text-cente mt-4">

    <div class="col-12 mb-4"><br><br>
        <h1 class="text-center">{{user.pseudo}} comments: {{ user.comments|length}} comments</h1><br><br>
    </div><br><br>
    {% for comment in user.comments %}
    <table class="table table-striped table-responsive">
        <thead>
            <tr>
                <th>Member</th>
                <th>Content</th>
                <th>Trick</th>
                <th>Date of création</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{user.pseudo}}</td>
                <td>{{comment.content}}</td>
                <td>{{comment.trick.name}}</td>
                <td>{{comment.creationDate|date('d/m/Y')}}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}